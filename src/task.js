
class todo {
    constructor(title,note,due,prio) {
        this.title = form.title.value;
        //testing out class and constructor prefer this over function
    }
}
function todos() {
    title = this.title;
    note = this.note;
    due = this.due;
    prio = this.prio;
}

const todo = (() => {

    const todoArr = [];
    // displaying array plan the functions so its not mumbo jumbo tmr


})();

export default todos();